node_modules/
.wrangler/
.env
.env.*
.dev.vars
dist/
.next/
.DS_Store
Thumbs.db

# 브라우저에서 저장된 메모/자격증명 파일이 실수로 커밋되지 않도록 차단
http*.txt
*_login.txt
*비밀번호*
*password*.txt

# docs/ 는 2026-07-27 부터 공개 저장소에 포함한다.
# (검토에서 지적된 항목이 조치 완료됐고, 남은 서술은 자격증명이 아니라 설정 설명임)
# 다만 아래는 계속 제외한다.
#  - zip: 저장소 내용을 그대로 담은 중복본이라 곧 어긋난다. 필요할 때 다시 만들면 된다.
E-PocketBase/docs/*.zip

# 진동시험기 예약 사이트 (A - 구글시트 버전) 설치 방법 — 코드 포함 통합본

> 이 문서 하나만 위에서부터 순서대로 따라 하면 됩니다.
> **Code.gs / index.html 전체 코드가 이 문서 안에 다 들어 있습니다.** (별도 파일을 열 필요 없음)
> 테스트 계정: 구글 계정 **att.cantops@gmail.com**
> 프로그램 설치·서버·비용 전혀 없습니다. 아래 순서대로 10~15분이면 링크가 나옵니다.

---

## ⏱️ 전체 순서 한눈에
1. 구글시트 새로 만들기
2. Apps Script(스크립트 편집기) 열기
3. **[코드 ①] Code.gs** 붙여넣기
4. **[코드 ②] index.html** 파일 추가 후 붙여넣기
5. 메일 권한 1회 승인 (`testMail` 실행)
6. 시작 전 자동 알림 트리거 만들기 (`setupReminderTrigger` 실행)
7. 웹앱으로 배포 → **링크 나옴**
8. 두 층에서 접속 테스트

---

## 1단계 — 구글시트 새로 만들기
1. `att.cantops@gmail.com` 으로 구글 로그인
2. 브라우저 주소창에 **sheets.new** 입력 → 빈 스프레드시트가 열림
3. 왼쪽 위 파일 이름을 `진동시험기 예약` 으로 바꿈

## 2단계 — 스크립트 편집기 열기
1. 상단 메뉴 **확장 프로그램 → Apps Script** 클릭
2. 새 탭에 코드 편집기가 열림 (`Code.gs` 라는 파일이 보임)

## 3단계 — 백엔드 코드 붙여넣기  ← [코드 ①]
1. 편집기에 기본으로 있는 `function myFunction() {}` 내용을 **전부 지움**
2. 아래 **[코드 ① Code.gs]** 블록 전체를 복사해 붙여넣기
3. 상단 💾(저장) 아이콘 클릭

## 4단계 — 화면(HTML) 파일 추가  ← [코드 ②]
1. 편집기 왼쪽 파일 목록 옆 **＋ → HTML** 클릭
2. 파일 이름을 정확히 **`index`** 로 입력 (자동으로 `index.html` 이 됨)
3. 새로 생긴 파일의 내용을 **전부 지우고**, 아래 **[코드 ② index.html]** 블록 전체를 복사해 붙여넣기
4. 💾 저장

> ⚠️ 파일 이름은 반드시 소문자 **index** 여야 합니다. (Code.gs 안에서 'index' 를 불러오기 때문)

## 5단계 — 메일 권한 1회 승인 (한 번만)
1. 편집기 상단 함수 선택 칸에서 **`testMail`** 선택 → **▶ 실행**
2. 권한 승인 창이 뜸:
   - "권한 검토" → 계정(`att.cantops@gmail.com`) 선택 → **"고급" → "(안전하지 않음)... 이동"** → **허용**
   - (내가 만든 스크립트라 나오는 정상 경고입니다)
3. `btkang@cantops.biz` 로 테스트 메일이 오면 정상. **이 승인을 안 하면 예약 알림 메일이 조용히 안 갑니다.**

> ℹ️ 시간대 확인: 편집기 좌측 **⚙️ 프로젝트 설정 → 시간대**가 `(GMT+09:00) 서울` 인지 확인하세요. 아니면 알림 시각이 어긋납니다.

## 6단계 — 시작 30분 전 자동 알림 켜기 (한 번만)
1. 함수 선택 칸에서 **`setupReminderTrigger`** 선택 → **▶ 실행**
2. 이걸 실행하면 5분마다 도는 알림 트리거가 만들어집니다. (예약 시작 30분 전 예약자에게 메일)

## 7단계 — 웹앱으로 배포하기
1. 오른쪽 위 **배포 → 새 배포** 클릭
2. 톱니바퀴(⚙️) → **웹 앱** 선택
3. 설정:
   - 설명: `진동시험기 예약` (아무거나)
   - 실행 계정: **나(att.cantops@gmail.com)**
   - 액세스 권한: **모든 사용자** ← 두 층 누구나 링크로 들어오게 하려면 이걸로
4. **배포** 클릭
5. 완료되면 **웹 앱 URL**(`.../exec` 로 끝남)이 나옵니다 → 이게 예약 링크! 복사해 두세요.

## 8단계 — 테스트
1. 나온 링크를 새 탭/휴대폰에서 열어 예약이 되는지 확인
2. 예약하면 구글시트 **'예약내역'** 시트에 자동으로 줄이 쌓입니다
3. 관리자는 그 시트에서 직접 보고/수정/삭제 가능

> 💡 다중 구글 로그인 브라우저에서 화면이 안 열리면 **시크릿(익명) 창**에서 열어보세요. (구글 알려진 버그)

---

## 두 층에서 접속 테스트
- 이 링크는 **인터넷 주소(https://script.google.com/...)** 라서, 10층·12층 모두 **인터넷만 되면 접속됩니다.**
- `\\`(파일공유)가 막혀 있어도 상관없습니다. 웹은 인터넷으로 도니까요.
- 10층에서 한 번, 12층에서 한 번 열어보면 끝.

## 운영 방식 바꾸기 (Code.gs 맨 위 `CONFIG`)
```js
const CONFIG = {
  machines: ['진동시험기 1호'],   // 장비 늘리려면 ['진동시험기 1호','진동시험기 2호'] 처럼
  startHour: 8,                   // 예약 가능 시작 (0~23)
  endHour: 20,                    // 예약 가능 종료 (1~24)
  maxHours: 8,                    // 한 번에 예약 가능한 최대 연속 시간
  sheetName: '예약내역',
  adminEmail: 'btkang@cantops.biz', // 담당자 메일(예약 알림 수신). 비우면 담당자 알림 안 감
  notifyAdmin: true,              // 예약/취소 시 담당자에게 메일
  notifyReserver: true,           // 예약자가 이메일 입력 시 확인 메일
  reminderMinutes: 30,            // 예약 시작 몇 분 전에 알림
  remindAdmin: false,             // 시작 전 알림을 담당자에게도 보낼지
};
```
- 값을 바꾼 뒤 **배포 → 배포 관리 → 연필(수정) → 버전: 새 버전 → 배포** 하면 반영됩니다.
  (링크는 그대로 유지됩니다)

## 자주 나오는 문제
| 증상 | 해결 |
|------|------|
| 화면이 안 뜨고 오류 | HTML 파일 이름이 소문자 `index` 인지 확인 |
| 예약은 되는데 메일이 안 옴 | 5단계 **메일 권한 승인(`testMail`)** 을 했는지 확인 |
| 시작 전 알림이 안 옴 | 6단계 **`setupReminderTrigger`** 를 실행했는지 + 예약 시 이메일을 입력했는지 확인 |
| 알림 시각이 이상함 | 프로젝트 설정 → 시간대가 **서울** 인지 확인 |
| "권한이 없습니다" | 배포 시 액세스 권한을 **모든 사용자** 로 했는지 확인 |
| 코드 바꿨는데 반영 안 됨 | 배포 관리에서 **새 버전** 으로 다시 배포 |
| 다른 사람이 접속 안 됨 | 링크 끝이 `/exec` 인지 확인 (`/dev` 는 본인만 됨) |

---
---

# [코드 ① Code.gs]  ← 3단계에서 붙여넣기

```js
/**
 * 진동시험기 예약 사이트 - 구글 Apps Script 백엔드 (영문 함수명 안전판)
 * ------------------------------------------------------------
 * - 구글 스프레드시트를 데이터베이스로 사용합니다.
 * - 링크(웹앱 URL) 하나로 10층/12층 어디서든 예약할 수 있습니다.
 * - 예약 내역은 연결된 구글시트에서 관리자가 바로 보고 수정할 수 있습니다.
 *
 * ▼ 여기 CONFIG 만 바꾸면 운영 방식을 조정할 수 있습니다.
 */
const CONFIG = {
  machines: ['진동시험기 1호'],   // 장비 여러 대면 ['진동시험기 1호','진동시험기 2호'] 처럼 추가
  startHour: 8,                   // 예약 가능 시작 시각 (0~23)
  endHour: 20,                    // 예약 가능 종료 시각 (1~24). 8~20 이면 08:00~20:00
  maxHours: 8,                    // 한 번에 예약 가능한 최대 시간(연속)
  sheetName: '예약내역',

  // ▼ 메일 알림 설정 ---------------------------------------------
  adminEmail: 'btkang@cantops.biz', // 담당자 메일(여기로 예약 알림이 옴). 비우면 담당자 알림 안 감
  notifyAdmin: true,              // 예약/취소 시 담당자에게 메일
  notifyReserver: true,           // 예약자가 이메일을 입력하면 확인 메일 발송

  reminderMinutes: 30,            // 예약 시작 몇 분 전에 알림 메일을 보낼지
  remindAdmin: false,             // 시작 전 알림을 담당자에게도 보낼지(true면 담당자도 받음)
};

/** 시각(숫자)을 'HH:00' 문자열로. */
function hhmm(h) { return ('0' + h).slice(-2) + ':00'; }

/** 예약 1건을 사람이 읽기 좋은 본문으로. */
function buildBody(machine, date, hours, name, dept, memo) {
  const s = Math.min.apply(null, hours), e = Math.max.apply(null, hours) + 1;
  let body = '예약자 : ' + name + (dept ? ' (' + dept + ')' : '') + '\n';
  body += '장비 : ' + machine + '\n';
  body += '날짜 : ' + date + '\n';
  body += '시간 : ' + hhmm(s) + ' ~ ' + hhmm(e) + '\n';
  if (memo) body += '메모 : ' + memo + '\n';
  return body;
}

/** 메일 발송(실패해도 예약 자체는 유지). */
function sendMail_(to, subject, body) {
  if (!to) return;
  try { MailApp.sendEmail(String(to).trim(), subject, body); } catch (e) { /* 메일 실패 무시 */ }
}

/** 시간 배열이 '연속 + 최대시간 이내'인지 검사합니다. 문제 있으면 메시지를, 없으면 null 반환. */
function validateHours(hours) {
  if (!hours || hours.length === 0) return '시간을 선택해 주세요.';
  if (hours.length > CONFIG.maxHours) return '최대 ' + CONFIG.maxHours + '시간까지 예약할 수 있습니다.';
  const sorted = hours.slice().sort(function (a, b) { return a - b; });
  for (let i = 1; i < sorted.length; i++) {
    if (sorted[i] !== sorted[i - 1] + 1) return '연속된 시간만 예약할 수 있습니다.';
  }
  return null;
}

/** 웹앱에 접속하면 예약 화면(index.html)을 보여줍니다. */
function doGet() {
  return HtmlService.createTemplateFromFile('index')
    .evaluate()
    .setTitle('진동시험기 예약')
    .addMetaTag('viewport', 'width=device-width, initial-scale=1')
    .setXFrameOptionsMode(HtmlService.XFrameOptionsMode.ALLOWALL);
}

/** 화면에서 설정값(기기목록/운영시간)을 읽어갈 때 사용합니다. */
function getConfig() {
  return {
    machines: CONFIG.machines,
    startHour: CONFIG.startHour,
    endHour: CONFIG.endHour,
    maxHours: CONFIG.maxHours,
  };
}

/** 예약 데이터를 저장할 시트를 준비합니다(없으면 자동 생성 + 머리글 작성). */
function ensureSheet() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  let sheet = ss.getSheetByName(CONFIG.sheetName);
  if (!sheet) {
    sheet = ss.insertSheet(CONFIG.sheetName);
    sheet.appendRow(['ID', '기기', '날짜', '시', '예약자', '부서', '메모', '신청일시', '이메일', '알림']);
    sheet.setFrozenRows(1);
    sheet.getRange('A1:J1').setFontWeight('bold').setBackground('#eef2ff');
  }
  return sheet;
}

/**
 * 특정 기기 + 날짜의 예약 현황을 돌려줍니다.
 * 반환: { '9': {id, name, dept, memo}, '13': {...}, ... }  (시(hour)를 키로 사용)
 */
function getReservations(machine, date) {
  const sheet = ensureSheet();
  const rows = sheet.getDataRange().getValues();
  const result = {};
  for (let i = 1; i < rows.length; i++) {
    const id = rows[i][0];
    const rMachine = rows[i][1];
    const rDate = formatDate(rows[i][2]);
    const rHour = rows[i][3];
    const name = rows[i][4];
    const dept = rows[i][5];
    const memo = rows[i][6];
    if (rMachine === machine && rDate === date) {
      result[String(rHour)] = { id: id, name: name, dept: dept, memo: memo };
    }
  }
  return result;
}

/**
 * 예약을 저장합니다. 여러 시간대를 한 번에 예약할 수 있습니다.
 * data = { machine, date:'2026-07-22', hours:[9,10,11], name, dept, memo }
 * 이미 예약된 시간대가 하나라도 있으면 전체를 취소하고 알려줍니다(중복 방지).
 */
function addReservation(data) {
  const lock = LockService.getScriptLock();
  lock.waitLock(10000); // 두 사람이 동시에 예약해도 충돌 안 나게 잠금
  try {
    if (!data.name || !String(data.name).trim()) {
      return { ok: false, message: '예약자 이름을 입력해 주세요.' };
    }
    const hourError = validateHours(data.hours);
    if (hourError) return { ok: false, message: hourError };

    const current = getReservations(data.machine, data.date);
    const dup = data.hours.filter(function (h) { return current[String(h)]; });
    if (dup.length > 0) {
      const list = dup.map(function (h) { return h + '시'; }).join(', ');
      return { ok: false, message: '이미 예약된 시간대가 있습니다: ' + list + '\n새로고침 후 다시 시도해 주세요.' };
    }

    const sheet = ensureSheet();
    const now = new Date();
    const email = String(data.email || '').trim();
    data.hours.forEach(function (h) {
      const id = Utilities.getUuid().slice(0, 8);
      sheet.appendRow([
        id, data.machine, data.date, h,
        String(data.name).trim(),
        String(data.dept || '').trim(),
        String(data.memo || '').trim(),
        now, email,
      ]);
    });

    // 메일 알림
    const body = buildBody(data.machine, data.date, data.hours, data.name, data.dept, data.memo);
    if (CONFIG.notifyAdmin) sendMail_(CONFIG.adminEmail, '[진동시험기] 신규 예약 - ' + data.name, body);
    if (CONFIG.notifyReserver && email) sendMail_(email, '[진동시험기] 예약이 완료되었습니다', body);

    return { ok: true, message: '예약이 완료되었습니다.' };
  } finally {
    lock.releaseLock();
  }
}

/** 예약을 취소(삭제)합니다. 본인 확인은 간단히 이름 일치로 처리합니다. */
function cancelReservation(id, name) {
  const lock = LockService.getScriptLock();
  lock.waitLock(10000);
  try {
    const sheet = ensureSheet();
    const rows = sheet.getDataRange().getValues();
    for (let i = 1; i < rows.length; i++) {
      if (rows[i][0] === id) {
        if (String(rows[i][4]).trim() !== String(name).trim()) {
          return { ok: false, message: '예약자 이름이 일치해야 취소할 수 있습니다.' };
        }
        const rMachine = rows[i][1], rDate = formatDate(rows[i][2]), rHour = rows[i][3];
        const rName = rows[i][4], rDept = rows[i][5], rMemo = rows[i][6], rEmail = rows[i][8];
        sheet.deleteRow(i + 1);

        // 취소 메일 알림
        const body = buildBody(rMachine, rDate, [rHour], rName, rDept, rMemo);
        if (CONFIG.notifyAdmin) sendMail_(CONFIG.adminEmail, '[진동시험기] 예약 취소 - ' + rName, body);
        if (CONFIG.notifyReserver && rEmail) sendMail_(rEmail, '[진동시험기] 예약이 취소되었습니다', body);

        return { ok: true, message: '예약이 취소되었습니다.' };
      }
    }
    return { ok: false, message: '해당 예약을 찾을 수 없습니다.' };
  } finally {
    lock.releaseLock();
  }
}

/**
 * 반복 예약: 여러 날짜에 같은 시간대를 한 번에 예약합니다.
 * data = { machine, dates:['2026-08-04', ...], hours:[14,15], name, dept, memo }
 * 어떤 날짜에 시간이 하나라도 겹치면 그 날짜만 건너뛰고, 나머지는 예약합니다.
 */
function addRecurring(data) {
  const lock = LockService.getScriptLock();
  lock.waitLock(15000);
  try {
    if (!data.name || !String(data.name).trim()) return { ok: false, message: '예약자 이름을 입력해 주세요.' };
    if (!data.dates || data.dates.length === 0) return { ok: false, message: '예약할 날짜가 없습니다. 월/요일을 확인해 주세요.' };
    const hourError = validateHours(data.hours);
    if (hourError) return { ok: false, message: hourError };

    const sheet = ensureSheet();
    const rows = sheet.getDataRange().getValues();

    // 기존 예약을 (날짜|시) 형태로 한 번에 모읍니다.
    const taken = {};
    for (let i = 1; i < rows.length; i++) {
      if (rows[i][1] === data.machine) {
        taken[formatDate(rows[i][2]) + '|' + rows[i][3]] = true;
      }
    }

    const now = new Date();
    const email = String(data.email || '').trim();
    const bookedDates = [];
    const skipped = [];
    const toAppend = [];

    data.dates.forEach(function (date) {
      const conflict = data.hours.filter(function (h) { return taken[date + '|' + h]; });
      if (conflict.length > 0) {
        skipped.push(date);
      } else {
        data.hours.forEach(function (h) {
          const id = Utilities.getUuid().slice(0, 8);
          toAppend.push([
            id, data.machine, date, h,
            String(data.name).trim(), String(data.dept || '').trim(), String(data.memo || '').trim(), now, email,
          ]);
          taken[date + '|' + h] = true; // 같은 호출 내 중복도 방지
        });
        bookedDates.push(date);
      }
    });

    if (toAppend.length > 0) {
      sheet.getRange(sheet.getLastRow() + 1, 1, toAppend.length, 9).setValues(toAppend);
    }

    let message;
    if (bookedDates.length === 0) {
      message = '예약 가능한 날짜가 없습니다. (모두 이미 예약됨)';
    } else {
      message = bookedDates.length + '일 예약 완료.';
      if (skipped.length > 0) message += '\n이미 예약되어 건너뜀: ' + skipped.join(', ');
    }

    // 메일 알림 (반복 예약 요약)
    if (bookedDates.length > 0) {
      const s = Math.min.apply(null, data.hours), e = Math.max.apply(null, data.hours) + 1;
      let body = '예약자 : ' + data.name + (data.dept ? ' (' + data.dept + ')' : '') + '\n';
      body += '장비 : ' + data.machine + '\n';
      body += '시간 : 매 예약일 ' + hhmm(s) + ' ~ ' + hhmm(e) + '\n';
      body += '예약일(' + bookedDates.length + '일) : ' + bookedDates.join(', ') + '\n';
      if (skipped.length > 0) body += '건너뜀(이미 예약) : ' + skipped.join(', ') + '\n';
      if (data.memo) body += '메모 : ' + data.memo + '\n';
      if (CONFIG.notifyAdmin) sendMail_(CONFIG.adminEmail, '[진동시험기] 반복 예약 - ' + data.name, body);
      if (CONFIG.notifyReserver && email) sendMail_(email, '[진동시험기] 반복 예약이 완료되었습니다', body);
    }

    return { ok: true, bookedCount: bookedDates.length, skipped: skipped, message: message };
  } finally {
    lock.releaseLock();
  }
}

/**
 * (한 번만 실행) 5분마다 도는 알림 트리거를 만듭니다.
 * Apps Script 편집기에서 이 함수를 선택하고 ▶실행 하세요.
 */
function setupReminderTrigger() {
  ScriptApp.getProjectTriggers().forEach(function (t) {
    if (t.getHandlerFunction() === 'reminderCheck') ScriptApp.deleteTrigger(t);
  });
  ScriptApp.newTrigger('reminderCheck').timeBased().everyMinutes(5).create();
}

/**
 * (트리거가 자동 호출) 시작 reminderMinutes 분 전인 예약을 찾아 예약자에게 알림 메일을 보냅니다.
 * 같은 예약(같은 신청일시)은 한 번만 발송하고, '알림' 열에 Y 표시로 중복을 막습니다.
 */
function reminderCheck() {
  const sheet = ensureSheet();
  const rows = sheet.getDataRange().getValues();
  const now = new Date();
  const windowMs = CONFIG.reminderMinutes * 60 * 1000;

  // (기기|날짜|예약자|신청일시) 로 예약 건을 묶습니다.
  const groups = {};
  for (let i = 1; i < rows.length; i++) {
    const machine = rows[i][1];
    const date = formatDate(rows[i][2]);
    const hour = Number(rows[i][3]);
    const name = rows[i][4];
    const dept = rows[i][5];
    const memo = rows[i][6];
    const ts = rows[i][7] instanceof Date ? rows[i][7].getTime() : String(rows[i][7]);
    const email = rows[i][8];
    const flag = rows[i][9];
    const key = machine + '|' + date + '|' + name + '|' + ts;
    if (!groups[key]) {
      groups[key] = { rows: [], hours: [], machine: machine, date: date, name: name, dept: dept, memo: memo, email: email, reminded: false };
    }
    const gp = groups[key];
    gp.rows.push(i + 1);      // 실제 시트 행 번호
    gp.hours.push(hour);
    if (email) gp.email = email;
    if (String(flag) === 'Y') gp.reminded = true;
  }

  Object.keys(groups).forEach(function (key) {
    const gp = groups[key];
    if (gp.reminded || !gp.email) return;
    const startHour = Math.min.apply(null, gp.hours);
    const p = gp.date.split('-');
    const start = new Date(Number(p[0]), Number(p[1]) - 1, Number(p[2]), startHour, 0, 0);
    const diff = start.getTime() - now.getTime();
    if (diff > 0 && diff <= windowMs) {
      let body = buildBody(gp.machine, gp.date, gp.hours, gp.name, gp.dept, gp.memo);
      body += '\n※ 약 ' + CONFIG.reminderMinutes + '분 후 예약이 시작됩니다.';
      sendMail_(gp.email, '[진동시험기] 예약 시작 ' + CONFIG.reminderMinutes + '분 전 알림', body);
      if (CONFIG.remindAdmin) sendMail_(CONFIG.adminEmail, '[진동시험기] 곧 시작 - ' + gp.name, body);
      gp.rows.forEach(function (r) { sheet.getRange(r, 10).setValue('Y'); });
    }
  });
}

/** 날짜 값을 'YYYY-MM-DD' 문자열로 통일합니다. */
function formatDate(value) {
  if (value instanceof Date) {
    return Utilities.formatDate(value, Session.getScriptTimeZone(), 'yyyy-MM-dd');
  }
  return String(value);
}

/** (테스트용) 편집기에서 직접 실행해 메일 발송/권한을 확인합니다. */
function testMail() {
  Logger.log('남은 메일 발송 가능 수: ' + MailApp.getRemainingDailyQuota());
  MailApp.sendEmail(CONFIG.adminEmail, '[테스트] 진동시험기 메일', '메일 발송 테스트입니다. 이 메일이 오면 정상입니다.');
  Logger.log('발송 시도 완료');
}
```

---
---

# [코드 ② index.html]  ← 4단계에서 붙여넣기 (파일 이름: 소문자 index)

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <base target="_top">
  <style>
    * { box-sizing: border-box; }
    body { font-family: -apple-system, "Malgun Gothic", "맑은 고딕", sans-serif; margin: 0; background: #f4f6fb; color: #1f2937; }
    .wrap { max-width: 720px; margin: 0 auto; padding: 16px; }
    header { text-align: center; padding: 20px 0 8px; }
    header h1 { margin: 0; font-size: 22px; }
    header p { margin: 6px 0 0; color: #6b7280; font-size: 13px; }
    .card { background: #fff; border-radius: 14px; padding: 16px; box-shadow: 0 1px 3px rgba(0,0,0,.08); margin-bottom: 14px; }
    .row { display: flex; gap: 10px; flex-wrap: wrap; align-items: center; }
    label { font-size: 13px; color: #374151; font-weight: 600; }
    select, input[type=date], input[type=month], input[type=text] { padding: 10px 12px; border: 1px solid #d1d5db; border-radius: 10px; font-size: 15px; width: 100%; }
    .field { flex: 1; min-width: 120px; }

    .tabs { display: flex; gap: 6px; background: #eef2ff; padding: 4px; border-radius: 12px; }
    .tab { flex: 1; text-align: center; padding: 10px; border-radius: 9px; cursor: pointer; font-weight: 700; font-size: 14px; color: #6366f1; }
    .tab.on { background: #4f46e5; color: #fff; }

    .slots { display: grid; grid-template-columns: repeat(auto-fill, minmax(88px,1fr)); gap: 8px; margin-top: 12px; }
    .slot { border: 1.5px solid #d1d5db; border-radius: 10px; padding: 10px 6px; text-align: center; cursor: pointer; user-select: none; background: #fff; transition: .12s; }
    .slot .t { font-size: 15px; font-weight: 700; }
    .slot .s { font-size: 11px; color: #9ca3af; margin-top: 2px; }
    .slot.free:hover { border-color: #6366f1; }
    .slot.picked { border-color: #6366f1; background: #eef2ff; }
    .slot.picked .s { color: #6366f1; }
    .slot.taken { background: #fef2f2; border-color: #fecaca; cursor: not-allowed; }
    .slot.taken .t { color: #b91c1c; }
    .slot.taken .s { color: #ef4444; }

    .wdays { display: flex; gap: 6px; flex-wrap: wrap; margin-top: 6px; }
    .wd { display: none; }
    .wd + label { display: inline-block; padding: 8px 14px; border: 1.5px solid #d1d5db; border-radius: 999px; cursor: pointer; font-weight: 700; color: #374151; }
    .wd:checked + label { background: #eef2ff; border-color: #6366f1; color: #4f46e5; }

    .btn { background: #4f46e5; color: #fff; border: 0; border-radius: 10px; padding: 13px 18px; font-size: 16px; font-weight: 700; cursor: pointer; width: 100%; }
    .btn:disabled { background: #c7cbe0; cursor: not-allowed; }
    .btn.ghost { background: #fff; color: #4f46e5; border: 1.5px solid #c7d2fe; margin-top: 10px; }

    .msg { text-align: center; font-size: 14px; padding: 8px; min-height: 20px; }
    .msg.ok { color: #059669; white-space: pre-line; } .msg.err { color: #dc2626; white-space: pre-line; }
    .hint { font-size: 12px; color: #9ca3af; margin-top: 8px; }
    .preview { font-size: 13px; color: #374151; margin-top: 10px; background: #f8fafc; border-radius: 10px; padding: 10px; min-height: 10px; }
    .loading { text-align:center; color:#9ca3af; padding: 20px; }
    .mylist { font-size: 13px; }
    .mylist .item { display:flex; justify-content:space-between; align-items:center; padding: 8px 0; border-bottom: 1px solid #f0f0f0; }
    .mylist .item:last-child { border-bottom: 0; }
    .cancel { color:#dc2626; background:none; border:0; font-size:13px; cursor:pointer; text-decoration:underline; }
  </style>
</head>
<body>
  <div class="wrap">
    <header>
      <h1>🔧 진동시험기 예약</h1>
      <p>날짜와 시간을 선택해 예약하세요 (1시간 단위)</p>
    </header>

    <div class="card">
      <div class="tabs">
        <div class="tab on" id="tabNormal" onclick="setMode('normal')">일반 예약</div>
        <div class="tab" id="tabRec" onclick="setMode('rec')">반복 예약</div>
      </div>
      <div style="margin-top:12px">
        <label>장비</label>
        <select id="machine"></select>
      </div>
    </div>

    <!-- 일반 예약 -->
    <div class="card" id="normalPanel">
      <label>날짜</label>
      <input type="date" id="date">
      <div id="slotArea"><div class="loading">불러오는 중…</div></div>
      <div class="hint">🟦 선택됨 · 🟥 예약됨(클릭 불가) · <b>연속된 시간만, 최대 8시간</b>까지 선택할 수 있습니다.</div>
    </div>

    <!-- 반복 예약 -->
    <div class="card" id="recPanel" style="display:none">
      <div class="row">
        <div class="field"><label>시작 날짜</label><input type="date" id="recFrom"></div>
        <div class="field"><label>종료 날짜</label><input type="date" id="recTo"></div>
      </div>
      <div class="row" style="margin-top:10px">
        <div class="field"><label>시작 시각</label><select id="recStart"></select></div>
        <div class="field"><label>길이(시간)</label><select id="recLen"></select></div>
      </div>
      <div style="margin-top:12px">
        <label>요일 필터 (선택 안 하면 기간 내 매일)</label>
        <div class="wdays" id="wdays"></div>
      </div>
      <button class="btn ghost" onclick="previewRecurring()">미리보기</button>
      <div class="preview" id="recPreview">기간(과 요일)을 고른 뒤 미리보기를 눌러보세요.</div>
    </div>

    <!-- 예약자 정보 + 예약 버튼 (공통) -->
    <div class="card">
      <div class="row">
        <div class="field"><label>예약자 이름 *</label><input type="text" id="name" placeholder="예: 홍길동"></div>
        <div class="field"><label>부서 / 연락처</label><input type="text" id="dept" placeholder="예: 품질팀 / 내선 1234"></div>
      </div>
      <div style="margin-top:10px"><label>이메일 (선택 · 입력 시 확인 메일 발송)</label><input type="text" id="email" placeholder="예: hong@cantops.biz"></div>
      <div style="margin-top:10px"><label>메모 (선택)</label><input type="text" id="memo" placeholder="예: 24시간 연속 시험"></div>
      <div class="msg" id="msg"></div>
      <button class="btn" id="submitBtn" onclick="submit()">선택한 시간 예약하기</button>
    </div>

    <!-- 현황 목록 (일반 예약일 때만) -->
    <div class="card" id="listCard">
      <label>이 날짜의 예약 현황</label>
      <div class="mylist" id="list"></div>
    </div>
  </div>

  <script>
    var config = null, pickedHours = [], current = {}, mode = 'normal';
    var recDates = [], recHours = [], slotEls = {};
    var WD = ['일', '월', '화', '수', '목', '금', '토'];

    google.script.run.withSuccessHandler(onConfig)
      .withFailureHandler(function (err) {
        document.getElementById('slotArea').innerHTML = '<div class="msg err">설정을 불러오지 못했습니다: ' + err.message + '</div>';
      }).getConfig();

    function onConfig(s) {
      config = s;
      var machine = document.getElementById('machine');
      s.machines.forEach(function (m) { var o = document.createElement('option'); o.value = m; o.textContent = m; machine.appendChild(o); });

      var t = new Date();
      var todayStr = t.getFullYear() + '-' + String(t.getMonth() + 1).padStart(2, '0') + '-' + String(t.getDate()).padStart(2, '0');
      var d = document.getElementById('date');
      d.value = todayStr; d.min = todayStr;
      var fromEl = document.getElementById('recFrom'), toEl = document.getElementById('recTo');
      fromEl.value = todayStr; fromEl.min = todayStr;
      toEl.value = todayStr; toEl.min = todayStr;

      // 시작 시각 / 길이 옵션
      var start = document.getElementById('recStart');
      for (var h = s.startHour; h < s.endHour; h++) {
        var o = document.createElement('option'); o.value = h; o.textContent = String(h).padStart(2, '0') + ':00'; start.appendChild(o);
      }
      var len = document.getElementById('recLen');
      var maxLen = Math.min(s.endHour - s.startHour, s.maxHours);
      for (var L = 1; L <= maxLen; L++) {
        var o2 = document.createElement('option'); o2.value = L; o2.textContent = L + '시간'; len.appendChild(o2);
      }

      // 요일 체크박스
      var wdays = document.getElementById('wdays');
      for (var i = 0; i < 7; i++) {
        var id = 'wd' + i;
        var cb = document.createElement('input'); cb.type = 'checkbox'; cb.className = 'wd'; cb.id = id; cb.value = i;
        var lb = document.createElement('label'); lb.setAttribute('for', id); lb.textContent = WD[i];
        wdays.appendChild(cb); wdays.appendChild(lb);
      }

      machine.onchange = function () { if (mode === 'normal') loadReservations(); };
      d.onchange = loadReservations;
      loadReservations();
    }

    function setMode(m) {
      mode = m;
      document.getElementById('tabNormal').classList.toggle('on', m === 'normal');
      document.getElementById('tabRec').classList.toggle('on', m === 'rec');
      document.getElementById('normalPanel').style.display = m === 'normal' ? '' : 'none';
      document.getElementById('recPanel').style.display = m === 'rec' ? '' : 'none';
      document.getElementById('listCard').style.display = m === 'normal' ? '' : 'none';
      document.getElementById('submitBtn').textContent = m === 'normal' ? '선택한 시간 예약하기' : '반복 예약하기';
      document.getElementById('msg').textContent = '';
      if (m === 'normal') loadReservations();
    }

    function submit() { if (mode === 'normal') reserve(); else reserveRecurring(); }

    // ---------- 일반 예약 ----------
    function loadReservations() {
      pickedHours = [];
      document.getElementById('slotArea').innerHTML = '<div class="loading">불러오는 중…</div>';
      google.script.run.withSuccessHandler(function (res) { current = res; drawSlots(); drawList(); })
        .withFailureHandler(function (err) {
          document.getElementById('slotArea').innerHTML = '<div class="msg err">현황을 불러오지 못했습니다: ' + err.message + '</div>';
        }).getReservations(document.getElementById('machine').value, document.getElementById('date').value);
    }

    function drawSlots() {
      var area = document.getElementById('slotArea'), g = document.createElement('div');
      g.className = 'slots';
      slotEls = {};
      for (var h = config.startHour; h < config.endHour; h++) {
        var div = document.createElement('div'), r = current[String(h)];
        var label = String(h).padStart(2, '0') + ':00';
        if (r) {
          div.className = 'slot taken';
          div.innerHTML = '<div class="t">' + label + '</div><div class="s">' + (r.name || '예약됨') + '</div>';
        } else {
          div.className = 'slot free';
          div.innerHTML = '<div class="t">' + label + '</div><div class="s">가능</div>';
          slotEls[h] = div;
          (function (hour) { div.onclick = function () { toggleSlot(hour); }; })(h);
        }
        g.appendChild(div);
      }
      area.innerHTML = ''; area.appendChild(g);
    }

    // 연속된 시간만, 최대 config.maxHours 시간까지 선택되게 합니다.
    function toggleSlot(h) {
      var msg = document.getElementById('msg'); msg.textContent = '';
      var idx = pickedHours.indexOf(h);
      if (idx >= 0) {
        var mn = Math.min.apply(null, pickedHours), mx = Math.max.apply(null, pickedHours);
        if (h === mn || h === mx) { pickedHours.splice(idx, 1); } // 양 끝만 해제 가능
        else { pickedHours = []; }                                // 가운데를 누르면 선택 초기화
      } else if (pickedHours.length === 0) {
        pickedHours = [h];
      } else {
        var lo = Math.min.apply(null, pickedHours), hi = Math.max.apply(null, pickedHours);
        if (h === lo - 1 || h === hi + 1) {                       // 블록에 이어붙는 경우만 연장
          if (pickedHours.length >= config.maxHours) {
            msg.className = 'msg err'; msg.textContent = '최대 ' + config.maxHours + '시간까지 예약할 수 있습니다.';
            return;
          }
          pickedHours.push(h);
        } else {
          pickedHours = [h];                                      // 떨어진 칸을 누르면 새 블록 시작
        }
      }
      applyPicked();
    }

    function applyPicked() {
      for (var hh in slotEls) {
        slotEls[hh].classList.toggle('picked', pickedHours.indexOf(Number(hh)) >= 0);
      }
    }

    function drawList() {
      var list = document.getElementById('list');
      var hours = Object.keys(current).map(Number).sort(function (a, b) { return a - b; });
      if (hours.length === 0) { list.innerHTML = '<div style="color:#9ca3af;padding:6px 0">아직 예약이 없습니다.</div>'; return; }
      list.innerHTML = '';
      hours.forEach(function (h) {
        var r = current[String(h)], item = document.createElement('div');
        item.className = 'item';
        var left = document.createElement('div');
        left.innerHTML = '<b>' + String(h).padStart(2, '0') + ':00</b> · ' + r.name +
          (r.dept ? ' <span style="color:#9ca3af">(' + r.dept + ')</span>' : '') +
          (r.memo ? '<br><span style="color:#9ca3af;font-size:12px">' + r.memo + '</span>' : '');
        var cancel = document.createElement('button');
        cancel.className = 'cancel'; cancel.textContent = '취소';
        (function (id) { cancel.onclick = function () { cancelBooking(id); }; })(r.id);
        item.appendChild(left); item.appendChild(cancel); list.appendChild(item);
      });
    }

    function reserve() {
      var msg = document.getElementById('msg'), name = document.getElementById('name').value.trim();
      if (!name) { msg.className = 'msg err'; msg.textContent = '예약자 이름을 입력해 주세요.'; return; }
      if (pickedHours.length === 0) { msg.className = 'msg err'; msg.textContent = '시간대를 선택해 주세요.'; return; }
      var btn = document.getElementById('submitBtn');
      btn.disabled = true; msg.className = 'msg'; msg.textContent = '예약 처리 중…';
      google.script.run.withSuccessHandler(function (res) {
        btn.disabled = false; msg.className = 'msg ' + (res.ok ? 'ok' : 'err'); msg.textContent = res.message;
        if (res.ok) { document.getElementById('memo').value = ''; loadReservations(); }
      }).withFailureHandler(function (err) {
        btn.disabled = false; msg.className = 'msg err'; msg.textContent = '오류: ' + err.message;
      }).addReservation({
        machine: document.getElementById('machine').value, date: document.getElementById('date').value,
        hours: pickedHours, name: name,
        dept: document.getElementById('dept').value, memo: document.getElementById('memo').value,
        email: document.getElementById('email').value,
      });
    }

    function cancelBooking(id) {
      var input = prompt('예약을 취소하려면 예약자 이름을 입력하세요.');
      if (input === null) return;
      google.script.run.withSuccessHandler(function (res) {
        var msg = document.getElementById('msg');
        msg.className = 'msg ' + (res.ok ? 'ok' : 'err'); msg.textContent = res.message;
        if (res.ok) loadReservations();
      }).withFailureHandler(function (err) {
        var msg = document.getElementById('msg'); msg.className = 'msg err'; msg.textContent = '오류: ' + err.message;
      }).cancelReservation(id, input);
    }

    // ---------- 반복 예약 ----------
    function computeRecurring() {
      var from = document.getElementById('recFrom').value;
      var to = document.getElementById('recTo').value;
      if (!from || !to) return { dates: [], hours: [], error: '시작/종료 날짜를 선택해 주세요.' };
      if (from > to) return { dates: [], hours: [], error: '종료 날짜가 시작 날짜보다 빠릅니다.' };

      var weekdays = [];
      for (var i = 0; i < 7; i++) { if (document.getElementById('wd' + i).checked) weekdays.push(i); }

      var start = +document.getElementById('recStart').value;
      var len = +document.getElementById('recLen').value;
      if (start + len > config.endHour) return { dates: [], hours: [], error: '길이가 운영시간을 넘습니다. (' + String(config.endHour).padStart(2,'0') + ':00 까지)' };
      var hours = [];
      for (var h = start; h < start + len; h++) hours.push(h);

      // 기간(시작~종료)을 하루씩 순회. 요일 필터가 비어 있으면 매일 포함.
      var fp = from.split('-'), tp = to.split('-');
      var cur = new Date(+fp[0], +fp[1] - 1, +fp[2]);
      var end = new Date(+tp[0], +tp[1] - 1, +tp[2]);
      var dates = [], guard = 0;
      while (cur.getTime() <= end.getTime() && guard < 400) {
        guard++;
        if (weekdays.length === 0 || weekdays.indexOf(cur.getDay()) >= 0) {
          dates.push(cur.getFullYear() + '-' + String(cur.getMonth() + 1).padStart(2, '0') + '-' + String(cur.getDate()).padStart(2, '0'));
        }
        cur.setDate(cur.getDate() + 1);
      }
      if (dates.length > 92) return { dates: [], hours: [], error: '기간이 너무 깁니다. 약 3개월(92일) 이내로 선택해 주세요.' };
      if (dates.length === 0) return { dates: [], hours: [], error: '해당 조건의 날짜가 없습니다.' };
      return { dates: dates, hours: hours, start: start, len: len };
    }

    function previewRecurring() {
      var msg = document.getElementById('msg'); msg.textContent = '';
      var r = computeRecurring();
      var box = document.getElementById('recPreview');
      if (r.error) { box.innerHTML = '<span style="color:#dc2626">' + r.error + '</span>'; recDates = []; return; }
      recDates = r.dates; recHours = r.hours;
      var timeStr = String(r.start).padStart(2, '0') + ':00 ~ ' + String(r.start + r.len).padStart(2, '0') + ':00';
      var days = r.dates.map(function (d) {
        var p = d.split('-'); return p[1] + '/' + p[2];
      }).join(', ');
      box.innerHTML = '<b>' + r.dates.length + '일</b> · 각 <b>' + timeStr + '</b><br>' + days;
    }

    function reserveRecurring() {
      var msg = document.getElementById('msg'), name = document.getElementById('name').value.trim();
      var r = computeRecurring();
      if (r.error) { msg.className = 'msg err'; msg.textContent = r.error; return; }
      if (!name) { msg.className = 'msg err'; msg.textContent = '예약자 이름을 입력해 주세요.'; return; }
      if (r.dates.length === 0) { msg.className = 'msg err'; msg.textContent = '해당 조건의 날짜가 없습니다.'; return; }
      if (!confirm(r.dates.length + '일을 예약합니다. 진행할까요?')) return;

      var btn = document.getElementById('submitBtn');
      btn.disabled = true; msg.className = 'msg'; msg.textContent = '예약 처리 중…';
      google.script.run.withSuccessHandler(function (res) {
        btn.disabled = false; msg.className = 'msg ' + (res.ok ? 'ok' : 'err'); msg.textContent = res.message;
      }).withFailureHandler(function (err) {
        btn.disabled = false; msg.className = 'msg err'; msg.textContent = '오류: ' + err.message;
      }).addRecurring({
        machine: document.getElementById('machine').value,
        dates: r.dates, hours: r.hours, name: name,
        dept: document.getElementById('dept').value, memo: document.getElementById('memo').value,
        email: document.getElementById('email').value,
      });
    }
  </script>
</body>
</html>
```
